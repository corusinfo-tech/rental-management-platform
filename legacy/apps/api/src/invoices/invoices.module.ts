import { Module } from '@nestjs/common'; import { BullModule } from '@nestjs/bullmq'; import { AuthModule } from '../auth/auth.module'; import { InvoiceController } from './invoices.controller'; import { InvoiceService } from './invoices.service'; import { InvoiceScheduler } from './invoice.scheduler';
@Module({ imports: [AuthModule, BullModule.forRoot({ connection: { url: process.env.REDIS_URL ?? 'redis://localhost:6379' } }), BullModule.registerQueue({ name: 'invoice-delivery' })], controllers: [InvoiceController], providers: [InvoiceService, InvoiceScheduler] }) export class InvoicesModule {}
