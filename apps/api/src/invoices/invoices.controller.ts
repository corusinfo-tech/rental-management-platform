import { Body, Controller, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { Role } from '@prisma/client'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { RolesGuard } from '../common/roles.guard'; import { Roles } from '../common/auth.decorator'; import { AuthUser, CurrentUser } from '../common/current-user.decorator'; import { GenerateInvoiceDto } from './invoices.dto'; import { InvoiceService } from './invoices.service';
@ApiTags('Invoice Generation') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Controller({ path: 'invoices', version: '1' }) export class InvoiceController { constructor(private service: InvoiceService) {} @Post('generate') @Roles(Role.ADMIN, Role.LANDLORD, Role.AGENT) generate(@CurrentUser() user: AuthUser, @Body() dto: GenerateInvoiceDto) { return this.service.generateManual(user.tenantId, user.sub, dto); } }
