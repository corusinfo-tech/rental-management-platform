{
  "name": "@noagent4u/scheduler",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch --noEmit -p tsconfig.json",
    "lint": "eslint ../../.eslintrc.cjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "bullmq": "^5.34.2",
    "ioredis": "^5.4.2"
  }
}
